export const routes = Object.freeze({
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
});

export const DASHBOARD_ROUTES = Object.freeze({
  HOME: "/",
  CREATE: "/create",
  LIST: "/travel-list",
  SETTINGS: "/settings",
});
